name: nisar-py
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10,<3.15
  - pip
  # For packaging, and testing
  - setuptools_scm
  - pytest
  - pytest-cov
  # For running
  - gdal
  - numpy
  - pystac
  - xarray
  - h5netcdf
  - pip:
      - -r requirements.txt
      - -r requirements-static.txt
